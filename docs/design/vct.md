# VCT 求解器（as-built · #70）

> 实现：`src/ai/vct.ts`。根策略见 [tang-seng-strength.md](./tang-seng-strength.md)；VCF 子集见 [vcf.md](./vcf.md)。  
> Issue：[#70](https://github.com/IdEvEbI/zen-gomoku/issues/70)。

| 项       | 决策 / 落地                                                                    |
| -------- | ------------------------------------------------------------------------------ |
| 状态     | **已实现**（`feature/vct-solver`）                                             |
| 野心     | **A**：可用中短 VCT，非完美长杀求解器                                          |
| 启用范围 | **仅唐僧**（`vctMaxPly=16`，`vctBudgetMs=1200`，`vctMaxNodes=80k`）；猪/悟为 0 |
| 骨架     | AND-OR；冲四线优先复用 VCF；活三/叉扩展；守方窄挡点 + 分支帽                   |
| API      | `findVctMove` / `findVctDefense` / `vctExists` / `hasVct`                      |

---

## 1. 定义与边界

| 概念      | 本仓含义                                  |
| --------- | ----------------------------------------- |
| **VCF**   | 连续冲四；VCT 的真子集                    |
| **VCT**   | 连续威胁：冲四 + 活三 + 叉，直至双杀/成五 |
| **与 αβ** | 根上战术探针；失败则降级软挡 / αβ         |

禁手：形检测与合法着依赖现有规则钩子；MVP 以 freestyle 回归为主。

---

## 2. 搜索结构

```txt
attackNode（OR）
  ├─ 若有冲四/胜点 → 先 vcfExists（短路）
  ├─ 候选：叉/冲四优先；有双杀启发则跳过全量活三打分
  └─ 试着 → defendNode

defendNode（AND）
  ├─ 守方一步胜 → 失败
  ├─ 攻方胜点≥2 或 可成活四点≥2 → 胜（双威胁）
  ├─ 挡点 = 胜点 → 可成活四点 → 冲四点（窄于 listForcedReplies）
  ├─ 挡点过多 → 未证（失败，不误报胜）
  └─ 每个挡点后攻方仍 VCT → 胜
```

根上 `findVctMove` 用 `confirmRootVctAttack`：对**单冲四**挡唯一胜点后须仍有硬续，拦 220 的 `g6`；双活三（可成活四点≥2）按野心 A 放行。

### 2.1 根策略顺序（唐僧 · #81）

1. 一步胜 / 硬必防 / 己方活四 / 双胜点快路径
2. **己方 VCF**
3. **对方活四端**（`measureThreatResidual` 择优）→ **对方 VCF 必防**
4. 叉对杀抢攻（仅当对方无待破 VCF）
5. 对方叉：**统一强迫着**（冲四候选；`inspectForcingOutcome` 看应手后谁还有杀，优先保留己方 VCT）→ 否则软搜
6. **己方 VCT** → **对方 VCT 必防**
7. 其余软威胁 → αβ

相对 #70：活四端 / VCF 必防先于叉对杀；有叉时禁止「冲第一个四」假抢先（对局 2026-08-10-01-53-06：`f9` 优于 `e10`）。

### 2.2 参数（唐僧）

| 参数                        | 值                                                     |
| --------------------------- | ------------------------------------------------------ |
| `vcfMaxPly` / `vcfBudgetMs` | 14 / 300                                               |
| `vcfMaxNodes`               | 50_000                                                 |
| `vctMaxPly` / `vctBudgetMs` | 16 / 1200                                              |
| `vctMaxNodes`               | 80_000                                                 |
| 战术时限                    | 自 `getNextMove` 起 VCF 窗 + VCT 窗，且不超过总 1500ms |

---

## 3. 验收

- [x] `vct.test.ts`：覆盖 VCF 子集、非 VCF 的短 VCT、叉 VCT、根上 VCF 优先于软挡、VCF 必防
- [x] `vcf.test.ts` 不回归
- [x] 精选 VCT 题 fixtures + `npm run verify:vct`（#83；见 `fixtures/records/vct/`）
- [ ] 人机体感：唐僧中盘连续威胁

---

## 4. 参考（实现时对照）

Allis TSS 思想、RAOTS 着法分类、Rapfi「探针+αβ」、Pela 验题；根序教训（软着压过杀棋）已修。

猪/悟开 VCT、Worker、更深求解 → 另开 Issue（#83 结论：优先加深预算，再进 #74）。

---

## 修订记录

| 日期       | 说明                                                                        |
| ---------- | --------------------------------------------------------------------------- |
| 2026-08-07 | 设计草案：野心 A、仅唐僧                                                    |
| 2026-08-07 | as-built：`vct.ts` + 根序（VCF 必防先于慢 VCT）                             |
| 2026-08-07 | #81：残留威胁模型；活四先于 VCF 必防；己方 VCT 先于软叉                     |
| 2026-08-10 | #81：有叉时统一强迫着（应手后比 VCT，禁假冲四）                             |
| 2026-08-10 | #83：手拣题集 + `verify:vct` 能力表                                         |
| 2026-08-10 | #85：唐僧 VCT/VCF 预算与节点加深；嵌套 VCF 共用剩余节点                     |
| 2026-08-10 | 叉/冲四优先排序 + `confirmRootVctAttack` 拦单冲四假杀；220/221 → expect hit |
